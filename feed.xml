<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://fraolbatole.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://fraolbatole.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-08-01T17:33:17+00:00</updated><id>https://fraolbatole.github.io/feed.xml</id><title type="html">blank</title><subtitle>Fraol Batole — PhD student in Computer Science at Tulane University researching LLM-based agents and reliability for software engineering. </subtitle><entry><title type="html">AgentPrologKit — letting agents use Prolog without writing Prolog</title><link href="https://fraolbatole.github.io/blog/2026/agentprologkit/" rel="alternate" type="text/html" title="AgentPrologKit — letting agents use Prolog without writing Prolog"/><published>2026-07-25T14:00:00+00:00</published><updated>2026-07-25T14:00:00+00:00</updated><id>https://fraolbatole.github.io/blog/2026/agentprologkit</id><content type="html" xml:base="https://fraolbatole.github.io/blog/2026/agentprologkit/"><![CDATA[<p>In <a href="/assets/pdf/RefineAct.pdf">RefineAct</a>, the verifier sitting between an agent and each of its actions is backed by a Prolog engine. That choice is deliberate: a safety property over an agent’s trace is relational and ordered — “read a local file, then <em>later</em> make an outbound call” is a conjunction of goals with an ordering constraint — and unification with backtracking is exactly a search for whether that pattern holds. You declare what counts as unsafe and the engine finds it, returning either a witness or a definite no. On the ToolEmu benchmark that loop cut failure incidence from 77% to 39% while <em>raising</em> helpfulness from 1.0 to 1.9.</p> <p>I have since started other work that puts Prolog behind agents in the same way, and the recurring friction was never the logic. It was the plumbing.</p> <h2 id="what-actually-broke">What actually broke</h2> <p>Driving Prolog by shelling out <code class="language-plaintext highlighter-rouge">swipl -g "add_fact(...)" -g halt</code> means fighting shell quoting and Prolog term syntax at once, using strings — file paths, URLs, arbitrary tool-call arguments — that are hostile to both. The failures clustered into three kinds:</p> <ul> <li><strong>Syntax errors.</strong> The agent emits Prolog source text, and a stray quote or a malformed clause takes the file down. Worse, <code class="language-plaintext highlighter-rouge">swipl</code> consulting a bad file <em>skips the offending clause, prints to stderr, and reports success</em> — so the real error surfaces much later as an unrelated <code class="language-plaintext highlighter-rouge">existence_error</code>.</li> <li><strong>Silent misreads.</strong> A token starting with an uppercase letter is a logical variable, not an atom. Text that parses cleanly can mean something entirely different from what was intended, and a rule that unifies with too much makes a verifier stop discriminating without ever raising an error.</li> <li><strong>Getting stuck.</strong> A query that loops or backtracks forever just hangs, and the calling agent has nothing to act on.</li> </ul> <h2 id="the-library">The library</h2> <p><a href="https://github.com/fraolBatole/AgentPrologKit">AgentPrologKit</a> answers each of those directly.</p> <p>Terms are <strong>rendered from Python values, never parsed from strings</strong>. A plain <code class="language-plaintext highlighter-rouge">str</code> is always a quoted atom; the only way to get a logical variable is the explicit <code class="language-plaintext highlighter-rouge">Var("X")</code> sentinel. Escaping is correct by construction and the silent-misread case becomes unrepresentable rather than merely documented against.</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">agentprologkit</span> <span class="kn">import</span> <span class="n">KnowledgeBase</span><span class="p">,</span> <span class="n">Compound</span><span class="p">,</span> <span class="n">Var</span>

<span class="k">with</span> <span class="nc">KnowledgeBase</span><span class="p">(</span><span class="sh">"</span><span class="s">agent_run.pl</span><span class="sh">"</span><span class="p">)</span> <span class="k">as</span> <span class="n">kb</span><span class="p">:</span>
    <span class="n">kb</span><span class="p">.</span><span class="nf">add_fact</span><span class="p">(</span><span class="sh">"</span><span class="s">action</span><span class="sh">"</span><span class="p">,</span> <span class="p">[</span><span class="mi">1</span><span class="p">,</span> <span class="sh">"</span><span class="s">read_file</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">/home/o</span><span class="sh">'</span><span class="s">brien/.ssh/id_rsa</span><span class="sh">"</span><span class="p">])</span>
    <span class="n">kb</span><span class="p">.</span><span class="nf">add_fact</span><span class="p">(</span><span class="sh">"</span><span class="s">action</span><span class="sh">"</span><span class="p">,</span> <span class="p">[</span><span class="mi">2</span><span class="p">,</span> <span class="sh">"</span><span class="s">http_post</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">https://attacker.example.com</span><span class="sh">"</span><span class="p">])</span>

    <span class="n">kb</span><span class="p">.</span><span class="nf">add_rule</span><span class="p">(</span>
        <span class="nc">Compound</span><span class="p">(</span><span class="sh">"</span><span class="s">unsafe</span><span class="sh">"</span><span class="p">,</span> <span class="p">[</span><span class="nc">Var</span><span class="p">(</span><span class="sh">"</span><span class="s">Read</span><span class="sh">"</span><span class="p">)]),</span>
        <span class="p">[</span>
            <span class="nc">Compound</span><span class="p">(</span><span class="sh">"</span><span class="s">action</span><span class="sh">"</span><span class="p">,</span> <span class="p">[</span><span class="nc">Var</span><span class="p">(</span><span class="sh">"</span><span class="s">Read</span><span class="sh">"</span><span class="p">),</span> <span class="sh">"</span><span class="s">read_file</span><span class="sh">"</span><span class="p">,</span> <span class="nc">Var</span><span class="p">(</span><span class="sh">"</span><span class="s">_P</span><span class="sh">"</span><span class="p">)]),</span>
            <span class="nc">Compound</span><span class="p">(</span><span class="sh">"</span><span class="s">action</span><span class="sh">"</span><span class="p">,</span> <span class="p">[</span><span class="nc">Var</span><span class="p">(</span><span class="sh">"</span><span class="s">Send</span><span class="sh">"</span><span class="p">),</span> <span class="sh">"</span><span class="s">http_post</span><span class="sh">"</span><span class="p">,</span> <span class="nc">Var</span><span class="p">(</span><span class="sh">"</span><span class="s">_U</span><span class="sh">"</span><span class="p">)]),</span>
            <span class="nc">Compound</span><span class="p">(</span><span class="sh">"</span><span class="s">&lt;</span><span class="sh">"</span><span class="p">,</span> <span class="p">[</span><span class="nc">Var</span><span class="p">(</span><span class="sh">"</span><span class="s">Read</span><span class="sh">"</span><span class="p">),</span> <span class="nc">Var</span><span class="p">(</span><span class="sh">"</span><span class="s">Send</span><span class="sh">"</span><span class="p">)]),</span>
        <span class="p">],</span>
    <span class="p">)</span>

    <span class="n">kb</span><span class="p">.</span><span class="nf">query</span><span class="p">(</span><span class="sh">"</span><span class="s">unsafe(Step)</span><span class="sh">"</span><span class="p">)</span>  <span class="c1"># [{"Step": 1}]
</span></code></pre></div></div> <p>Loading fails <strong>loudly</strong> — the file is read term by term first, so a syntax error comes back with file, line, and column instead of a mystery failure downstream. Queries carry a timeout (10s default, configurable) so a runaway goal returns <code class="language-plaintext highlighter-rouge">QueryTimeout</code> rather than hanging. Results come back as <code class="language-plaintext highlighter-rouge">list[dict]</code> of bindings over SWI-Prolog’s Machine Query Interface, with no stdout parsing, and errors carry stable codes (<code class="language-plaintext highlighter-rouge">arity_mismatch</code>, <code class="language-plaintext highlighter-rouge">existence_error</code>, <code class="language-plaintext highlighter-rouge">term_error</code>) meant to be branched on rather than string-matched. An optional <code class="language-plaintext highlighter-rouge">Schema</code> pins predicate arities, which is useful because agents are good at inventing a plausible extra argument.</p> <p>Authoring needs no <code class="language-plaintext highlighter-rouge">swipl</code> at all; only queries do.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>pip <span class="nb">install </span>agentprologkit   <span class="c"># plus SWI-Prolog &gt;= 8.4.2 on PATH, to query</span>
</code></pre></div></div> <p>The CLI mirrors the Python surface and emits one JSON object per command, shaped to map 1:1 onto MCP tools.</p> <p>The general point, which is really the same one as my <a href="/blog/2026/mcp-vs-api-with-skills/">note on MCP</a>: if you hand an agent a tool whose input language has silent failure modes, you have not given it a verifier. Making the wrong output impossible to express is a more reliable fix than asking for a better prompt.</p>]]></content><author><name></name></author><category term="research-notes"/><category term="agents"/><category term="prolog"/><category term="verification"/><category term="software-engineering"/><category term="refineact"/><summary type="html"><![CDATA[A small library that came out of RefineAct, where the bottleneck was not the logic but the agent's ability to produce valid Prolog and query it without getting stuck.]]></summary></entry><entry><title type="html">Why I stopped treating MCP as just another API wrapper</title><link href="https://fraolbatole.github.io/blog/2026/mcp-vs-api-with-skills/" rel="alternate" type="text/html" title="Why I stopped treating MCP as just another API wrapper"/><published>2026-06-25T14:00:00+00:00</published><updated>2026-06-25T14:00:00+00:00</updated><id>https://fraolbatole.github.io/blog/2026/mcp-vs-api-with-skills</id><content type="html" xml:base="https://fraolbatole.github.io/blog/2026/mcp-vs-api-with-skills/"><![CDATA[<p>There is a recurring argument online these days. One camp says MCP is just a thin wrapper and APIs are fine if you write good skills. The other says MCP is the future of agent tooling. After six months working on a research project that involves MCP, I think both camps are talking past each other.</p> <p>Both have their place. Skills work fine for simple, stable tools where the surface is small and the agent is unlikely to guess wrong. MCP pays off when you need precision, discoverability, and agents that do not guess. The real difference is not capability. It is what kind of thing you are handing the agent.</p> <p>Skills are documentation. MCP schema is a contract. That one distinction explains almost everything else.</p> <h2 id="documentation-versus-contracts">Documentation versus contracts</h2> <p>When you bolt a skill onto an API, you are writing a human-readable description of what the API does and hoping an agent reads it correctly. The skill has no binding relationship to the actual API behavior. It is a note. The agent can misread it, invent parameters that do not exist, or miss an edge case the skill author did not think to mention.</p> <p>I watched this happen on a small project before I switched to MCP. The agent kept making the same class of mistake on a nested parameter. The skill described the structure, but the description was not tight enough, and the agent filled in the gap with its best guess. Right intent, wrong shape, silent failure.</p> <p>Documentation is written to be read. A contract is written to be enforced. MCP changes the ground rule by being the latter. The tool definition, its parameters, and the error responses are machine-consumable by design. The agent is not interpreting a description. It is operating against a spec that was built to be machine-interpreted. That shift sounds small. It is not.</p> <h2 id="contracts-make-developers-more-careful">Contracts make developers more careful</h2> <p>This one surprised me. When you build an MCP server, you are forced to think carefully about what your tool actually does, what parameters are required versus optional, and what failure looks like at the boundary. That discipline produces better tools.</p> <p>Writing documentation has the opposite incentive. You describe just enough to make the agent probably work. There is no formal feedback loop telling you your description was ambiguous. The agent tries, fails in some subtle way, and you patch the skill with more text. Then more text. Then a disclaimer at the bottom.</p> <p>MCP developers are more cautious not because they are more careful people, but because a contract demands precision in a way that documentation never will.</p> <h2 id="contracts-are-searchable-documentation-is-not">Contracts are searchable, documentation is not</h2> <p>One thing I did not expect to matter as much as it does is discoverability. With an API and a set of skills, finding the right tool for a specific job is a problem the agent has to solve through prose. It reads descriptions, guesses which skill is relevant, and sometimes picks the wrong one.</p> <p>A contract has a known shape. MCP servers expose their tools in a structured, enumerable format. An agent can ask what tools exist, read their signatures, and reason about which one fits without guessing whether a skill even exists for this case. In a system with many tools spread across many servers, that difference compounds fast.</p> <h2 id="contracts-tell-you-what-went-wrong-documentation-does-not">Contracts tell you what went wrong, documentation does not</h2> <p>APIs return errors designed for developers who will read them, add logging, and figure out what went wrong. That is fine for a human. An agent needs something different.</p> <p>When something goes wrong in an MCP call, the response is structured in a way that tells the agent what happened and where. That closes a feedback loop that is left totally open in the documentation model. Without it, an agent hits a failure, gets back a generic error message, and either retries blindly or gives up. I saw that pattern enough times that it stopped feeling like bad luck and started feeling like a design flaw baked into the approach.</p> <h2 id="the-root-of-it">The root of it</h2> <p>APIs are designed with a human developer as the primary user. The choices made during API design, what to name a field, how to structure a response, where to put the edge cases, all of those choices are optimized for a person reading them. That is not a criticism. It is just a constraint.</p> <p>When you point an agent at a human-optimized API and hand it documentation, you are asking it to operate in a space built for a different kind of reader. Some of that works fine. A lot of it produces quiet, persistent failures that are hard to debug and easy to dismiss as the agent being dumb.</p> <p>MCP was built with agents as the primary user. It is a contract by design. And that changes the defaults in every small decision that follows.</p>]]></content><author><name></name></author><category term="research-notes"/><category term="mcp"/><category term="agents"/><category term="software-engineering"/><summary type="html"><![CDATA[Six months building with MCP changed how I think about the debate. The question is not API versus MCP. It is whether you want a contract or a prayer.]]></summary></entry><entry><title type="html">Keeping coding agents aligned with the original task</title><link href="https://fraolbatole.github.io/blog/2026/lightweight-agent-alignment/" rel="alternate" type="text/html" title="Keeping coding agents aligned with the original task"/><published>2026-06-18T14:00:00+00:00</published><updated>2026-06-18T14:00:00+00:00</updated><id>https://fraolbatole.github.io/blog/2026/lightweight-agent-alignment</id><content type="html" xml:base="https://fraolbatole.github.io/blog/2026/lightweight-agent-alignment/"><![CDATA[<p>When I was migrating parts of the Boa infrastructure to a new server, I kept running into the same problem with coding agents. The task was narrow: configure a service, adjust a deployment setting, inspect a failing command, or make one small change. The agent understood the request at the start, but the execution drifted.</p> <p>It would create files I did not ask for. It would modify files outside the scope of the task. It would hardcode table values in the database instead of fixing the configuration path that produced them. Sometimes it made changes that were not only unnecessary, but actively made the system harder to reason about.</p> <p>This happened most often when I used cheaper coding-agent modes after exhausting higher-quality tokens. The agent was not useless. It could make progress, but it did not reliably preserve the original intent as it moved from one tool call to the next.</p> <p>That distinction matters. A coding agent does not only answer a question. It acts on a codebase, a shell, a database, and sometimes a deployment environment. Once action is involved, being directionally helpful is not enough. The agent needs a check that asks a simpler question before each step:</p> <blockquote> <p>Is this proposed action still aligned with the original task?</p> </blockquote> <p>As a quick practical experiment, I tried a lightweight MCP guardrail that asked this question before each action was executed. It was useful as a sanity check and often worked better than a bare agent, but it was only a motivating prototype. The deeper problem is formal: once an agent can act, the system needs a principled way to decide whether the next action is permitted by the original intent.</p> <h2 id="the-failure-mode">The failure mode</h2> <p>The failure I saw during the Boa migration was not dramatic. It was ordinary drift.</p> <p>I would ask for one change, and the agent would infer a larger repair plan. I would ask it to inspect a configuration problem, and it would edit nearby files because they looked relevant. I would ask it to preserve the current data, and it would still try to patch the symptom by changing database contents.</p> <p>In isolation, each action looked defensible. In sequence, the behavior was wrong. The agent had substituted a local sense of progress for the actual user goal.</p> <p>Public incidents show the same shape at higher stakes. In the <a href="https://www.tomshardware.com/tech-industry/artificial-intelligence/ai-coding-platform-goes-rogue-during-code-freeze-and-deletes-entire-company-database-replit-ceo-apologizes-after-ai-engine-says-it-made-a-catastrophic-error-in-judgment-and-destroyed-all-production-data">reported Replit incident</a>, an agent deleted production data despite instructions that should have prevented destructive changes. <a href="https://www.theverge.com/ai-artificial-intelligence/897528/meta-rogue-ai-agent-security-incident">Reports about Meta</a> described another pattern: an internal assistant gave advice in the wrong context, contributing to a security incident, and a separate internal agent reportedly deleted emails without permission.</p> <p>The common issue is not only that agents make mistakes. The issue is that an agent can take an action that violates the task boundary while still appearing to work toward the task.</p> <h2 id="the-refineact-idea">The RefineAct idea</h2> <p>RefineAct is the formal approach that grew out of this concern: action-level alignment should be checked before execution, not only after a task succeeds or fails.</p> <p>RefineAct adds a runtime verification step between the agent’s proposed action and the environment that will execute it.</p> <p>The loop is simple:</p> <ol> <li>The user gives the original task.</li> <li>The agent proposes the next action.</li> <li>A verifier checks whether that action is consistent with the original task and its constraints.</li> <li>If the action is acceptable, it runs.</li> <li>If the action violates the task boundary, it is rejected and the agent must refine the action.</li> </ol> <p>The important part is where the check happens. RefineAct does not only inspect the final answer. It checks the action before it touches the system. The verifier gives the task intent an operational role: it becomes a constraint on the next action, not a sentence that only mattered at the beginning of the conversation.</p> <p>That placement matters for coding agents. A bad final answer is annoying. A bad shell command, file edit, database update, or deployment change can leave damage behind. Runtime verification gives the system a chance to stop the action before it becomes state.</p> <h2 id="takeaway">Takeaway</h2> <p>The main takeaway from RefineAct is that intent should remain active throughout the agent’s execution, not only at prompt time. A coding agent should be judged action by action against the task it was asked to perform.</p> <p>This is the core shift. Prompting tries to make the agent remember the goal. RefineAct gives the system a formal mechanism for checking whether the next action still satisfies that goal. The optional MCP guardrail showed the practical value of checking actions at runtime, but RefineAct is the principled version of that idea: a runtime-verification framework for keeping agent actions within the user’s intended boundary.</p>]]></content><author><name></name></author><category term="research-notes"/><category term="coding-agents"/><category term="software-engineering"/><category term="refineact"/><summary type="html"><![CDATA[The story behind RefineAct, a formal runtime-verification approach for checking whether each agent action still matches the user's intent.]]></summary></entry><entry><title type="html">Localizing design issues with LLM agents</title><link href="https://fraolbatole.github.io/blog/2026/localizing-design-issues-with-llm-agents/" rel="alternate" type="text/html" title="Localizing design issues with LLM agents"/><published>2026-05-20T13:00:00+00:00</published><updated>2026-05-20T13:00:00+00:00</updated><id>https://fraolbatole.github.io/blog/2026/localizing-design-issues-with-llm-agents</id><content type="html" xml:base="https://fraolbatole.github.io/blog/2026/localizing-design-issues-with-llm-agents/"><![CDATA[ <p>Most tooling for code quality is good at <em>detection</em> — it tells you a smell or a design problem exists. The harder, more useful question is <strong>localization</strong>: <em>where</em> in the code does the problem actually live, and what should I change?</p> <p>Design issues make this especially tricky. Unlike a null-pointer bug, a poor design decision is diffuse: it’s spread across several files, it depends on intent, and the “right” answer often requires understanding <em>why</em> the code is shaped the way it is. That context is exactly what classical static analysis throws away.</p> <h2 id="grounding-reasoning-in-program-facts">Grounding reasoning in program facts</h2> <p>The approach I’ve been working on treats this as a job for an <strong>agent</strong> that can do two things at once:</p> <ol> <li><strong>Look at concrete program facts</strong> — structure, dependencies, call relationships — the way a static analysis would.</li> <li><strong>Reason in natural language</strong> over those facts, the way a senior engineer reads a diff and says “this responsibility doesn’t belong here.”</li> </ol> <p>Neither half is enough alone. Pure analysis can’t weigh intent; a pure LLM hallucinates about code it can’t actually inspect. Putting the analysis <em>in the loop</em> keeps the agent honest, and lets it narrow a vague “something’s off in this module” down to the specific locations a developer should look at first.</p> <h2 id="why-localization-is-the-unlock">Why localization is the unlock</h2> <p>Localization is the bottleneck for everything downstream. If an agent can reliably say <em>where</em> the problem is, then automated refactoring, design repair, and review assistance all become tractable — they finally have a target to act on.</p> <p>This is the idea behind LocalizeAgent; the full method and evaluation are in the paper on my <a href="/publications/">publications</a> page. I’ll use future posts to dig into specific design decisions — how much analysis context to feed the agent, and how to keep its conclusions verifiable.</p>]]></content><author><name></name></author><category term="research-notes"/><category term="llm-agents"/><category term="software-engineering"/><summary type="html"><![CDATA[Why pinpointing where a design problem lives is harder than detecting that one exists — and how agents that combine program analysis with reasoning can help.]]></summary></entry></feed>